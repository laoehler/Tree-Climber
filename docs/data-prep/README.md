# Data Prep Assets

This folder contains non-runtime project artifacts used during data exploration and course catalog preparation.

- `Scraper.ipynb` documents the notebook workflow used to collect or inspect course data.
- `davidson_courses_meetings.csv` is a source dataset snapshot used during that process.

These files are intentionally separated from `src/` so the application code and supporting data-prep materials are easier to navigate.
